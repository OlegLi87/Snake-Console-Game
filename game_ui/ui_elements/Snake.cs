using game_engine;

namespace game_ui.ui_elements;

internal class Snake : SelfDrawableElement
{
    public Snake(char uiChar, Coordinate relativeCoordinate) : base(uiChar, relativeCoordinate)
    {
    }
}